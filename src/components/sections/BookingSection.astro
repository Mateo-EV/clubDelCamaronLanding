---
import { Image } from "astro:assets"
import SectionTitle from "../SectionTitle.astro"
import { Button } from "../ui/button"
import BookResturantImage from "@/assets/images/booking.jpg"
---

<section id="reservas" class="pt-40 space-y-12">
  <SectionTitle class="text-center">Reservas</SectionTitle>
  <div class="flex flex-col lg:flex-row container gap-8 lg:gap-0">
    <div class="flex-1 flex justify-center items-center order-2 lg:order-1">
      <Image
        src={BookResturantImage}
        alt="Club-del-camarón-local"
        class="rounded-md"
        width={500}
        data-aos="zoom-in"
      />
    </div>
    <div
      class="flex-1 space-y-6 text-xl text-muted-foreground order-1 lg:order-2"
      data-aos="fade-down"
    >
      <p class="font-semibold">
        *Las horas y/o días deshabilitados en el calendario no cuentan con
        disponibilidad.
      </p>
      <p>
        Si no encuentras espacio, contáctanos por WhatsApp al +51 973 079 806.
      </p>
      <p>
        La tolerancia es de 15 minutos y la duración de la reserva es de 120
        minutos.
      </p>
      <Button size="lg">Reservar Ahora</Button>
    </div>
  </div>
  <hr />
</section>
